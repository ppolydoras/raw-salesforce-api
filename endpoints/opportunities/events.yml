raw: "0.10"
endpoint: GET
metadata:
  title: Retrieve Opportunity Events.
  description: Retrieve events related to opportunities based on filters like opportunity name, opportunity id, and event subject.
  tags:
    - salesforce
    - event
    - opportunity
    - opportunity-event
code: sql
codeFile: events.sql
format: json
security:
  public: false
  scopes:
    - events:read
    - opportunity-events:read
computeClass: normal
enabled: true
