raw: "0.10"
endpoint: GET
metadata:
  title: Salesforce User Summary.
  description: Returns a summary of Salesforce users based on various filters and grouping options, including total users, active users, and users created within a specified date range.
  tags:
    - salesforce
    - user
    - summary
    - aggregate
code: sql
codeFile: summary.sql
format: json
security:
  public: false
  scopes:
    - users:read
computeClass: normal
enabled: true
expireSeconds: 90000
